# 📝 Инструкция по редактированию сайта

## 🎯 Как быстро и легко редактировать сайт

### 📋 Содержание:
1. [Изменить телефон](#изменить-телефон)
2. [Изменить email](#изменить-email)
3. [Изменить цены](#изменить-цены)
4. [Изменить текст на главной странице](#изменить-текст-на-главной)
5. [Добавить/изменить виды бытовок](#виды-бытовок)
6. [Изменить преимущества компании](#преимущества)
7. [Изменить часто задаваемые вопросы](#faq)
8. [Изменить этапы работы](#этапы-работы)
9. [Добавить фото](#добавить-фото)
10. [Как опубликовать изменения](#публикация)

---

## 🔧 Общий принцип работы

1. **Найдите нужный файл** (пути указаны ниже)
2. **Откройте его** в любом текстовом редакторе
3. **Измените нужный текст**
4. **Сохраните файл**
5. **Опубликуйте изменения** (см. раздел "Публикация")

---

## 📞 Изменить телефон

**Где находится:** Телефон используется во многих местах

### Быстрый способ - через поиск:

1. Откройте папку проекта в редакторе кода (VS Code, Cursor и т.д.)
2. Нажмите `Cmd+Shift+F` (Mac) или `Ctrl+Shift+F` (Windows) - это глобальный поиск
3. Введите текущий номер: `+7 917 909-88-84`
4. Замените на новый номер во всех найденных файлах
5. Не забудьте также заменить формат без пробелов: `+79179098884`

### Основные файлы где используется телефон:

```
src/components/Header.tsx          - шапка сайта
src/components/Footer.tsx          - подвал сайта  
src/components/Hero.tsx            - первый экран
src/components/FloatingButtons.tsx - липкие кнопки
src/app/contacts/page.tsx          - страница контактов
```

**Пример изменения в Header.tsx:**

Найдите (строка ~50):
```typescript
+7 917 909-88-84
```

Замените на:
```typescript
+7 (XXX) XXX-XX-XX
```

---

## 📧 Изменить email

**Где находится:** `info@mir-bytovok116.ru`

### Файлы:

```
src/components/Footer.tsx    - подвал сайта (строка ~88)
src/app/contacts/page.tsx    - страница контактов (строки 36, 65, 71, 77, 83)
src/app/privacy/page.tsx     - политика конфиденциальности (строка 132)
```

**Пример в Footer.tsx:**

Найдите:
```typescript
info@mir-bytovok116.ru
```

Замените на новый email:
```typescript
your-new-email@example.com
```

---

## 💰 Изменить цены

**Файл:** `src/components/PriceList.tsx`

### Как изменить:

1. Откройте файл `src/components/PriceList.tsx`
2. Найдите раздел `const prices = [` (начинается со строки 9)
3. Измените цены в нужных позициях

**Пример - изменить цену на утепленную бытовку:**

Найдите (строка ~25):
```typescript
{
  name: 'Бытовка 6х2.4м (утепленная)',
  price: 'от 220 000',
  features: ['Внутренняя отделка ДВП', 'Утепление 50мм', 'Электропроводка'],
  popular: true
},
```

Измените цену:
```typescript
price: 'от 250 000',  // Изменили цену
```

### Добавить новую позицию в прайс:

Скопируйте существующий блок и измените данные:
```typescript
{
  name: 'Ваше название',
  price: 'от XXX XXX',
  features: ['Особенность 1', 'Особенность 2', 'Особенность 3'],
  popular: false  // true если хотите пометить как "Популярное"
},
```

---

## 📄 Изменить текст на главной

**Файл:** `src/components/Hero.tsx`

### Изменить главный заголовок:

Найдите (строка ~30):
```typescript
<h1 className="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
  Производство, продажа и аренда бытовок
</h1>
```

Измените текст между `<h1>` и `</h1>`:
```typescript
<h1 className="text-4xl md:text-5xl lg:text-6xl font-bold mb-6 leading-tight">
  Ваш новый заголовок
</h1>
```

### Изменить подзаголовок:

Найдите (строка ~35):
```typescript
<p className="text-xl md:text-2xl text-industrial-100 mb-4">
  Доставка день в день. Собственное производство. Манипулятор в наличии.
</p>
```

Измените текст между `<p>` и `</p>`.

---

## 🏗️ Виды бытовок

**Файл:** `src/components/BytovkiTypes.tsx`

### Изменить существующий тип:

Найдите раздел `const types = [` (строка 8), затем нужный блок:

```typescript
{
  title: 'Строительные бытовки',
  description: 'Описание',
  features: ['Особенность 1', 'Особенность 2'],
  image: '/bytovka-stroitelnaya.svg',
  link: '/production'
},
```

Измените:
- `title` - название
- `description` - описание
- `features` - список особенностей (можно добавить/удалить)
- `image` - путь к картинке
- `link` - ссылка при клике

### Добавить новый тип бытовки:

Скопируйте один из блоков и добавьте после последнего:

```typescript
{
  title: 'Новый тип бытовки',
  description: 'Описание нового типа',
  features: ['Особенность 1', 'Особенность 2', 'Особенность 3'],
  image: '/путь-к-картинке.svg',
  link: '/production'
},
```

---

## ⭐ Преимущества

**Файл:** `src/components/Advantages.tsx`

### Изменить преимущество:

Найдите раздел `const advantages = [` (строка 20):

```typescript
{
  icon: (/* SVG иконка */),
  title: 'Быстрая доставка',
  description: 'Оперативная доставка бытовок по всей России'
},
```

Измените:
- `title` - заголовок преимущества
- `description` - описание

### Добавить новое преимущество:

Скопируйте блок и добавьте в конец списка. Иконку можно взять с [heroicons.com](https://heroicons.com).

---

## ❓ FAQ (Часто задаваемые вопросы)

**Файл:** `src/components/FAQ.tsx`

### Изменить вопрос и ответ:

Найдите раздел `const faqs = [` (строка 15):

```typescript
{
  question: 'Какие бытовки вы производите?',
  answer: 'Мы производим бытовки любого назначения...'
},
```

Измените текст в `question` и `answer`.

### Добавить новый вопрос:

Скопируйте блок и добавьте в конец:

```typescript
{
  question: 'Ваш новый вопрос?',
  answer: 'Подробный ответ на вопрос.'
},
```

---

## 🔄 Этапы работы

**Файл:** `src/components/HowWeWork.tsx`

### Изменить этап:

Найдите раздел `const steps = [` (строка 9):

```typescript
{
  number: '01',
  title: 'Заявка',
  description: 'Вы оставляете заявку...',
  icon: (/* SVG иконка */)
},
```

Измените:
- `title` - название этапа
- `description` - описание этапа

### Добавить/удалить этап:

Скопируйте блок для добавления или удалите ненужный.

---

## 📸 Добавить фото

### Фото бытовок (для галереи или видов):

1. Сохраните фото в папку `public/`
2. Назовите файл понятно, например: `bytovka-ofis.jpg`
3. В нужном компоненте укажите путь: `/bytovka-ofis.jpg`

**Пример в BytovkiTypes.tsx:**

```typescript
{
  title: 'Бытовка-офис',
  image: '/bytovka-ofis.jpg',  // Укажите имя вашего файла
  // остальные поля...
}
```

### Рекомендации по фото:

- **Формат:** JPG или PNG
- **Размер:** 800x600 пикселей (для карточек)
- **Вес:** не более 500KB (сжимайте на [tinypng.com](https://tinypng.com))
- **Качество:** четкие, хорошо освещенные фото

---

## 🚀 Публикация изменений

После того как внесли все изменения:

### Шаг 1: Сохраните файлы

Убедитесь, что все измененные файлы сохранены (Cmd+S / Ctrl+S).

### Шаг 2: Загрузите на GitHub

Откройте терминал и выполните:

```bash
cd /Users/user/Desktop/mir_bytovok116

# Добавить все изменения
git add .

# Создать коммит (опишите что изменили)
git commit -m "Изменены цены и контакты"

# Загрузить на GitHub
git push
```

### Шаг 3: Проверьте сайт

Через 1-2 минуты изменения появятся на сайте: **https://mir-bytovok116.ru**

---

## 🔍 Быстрый поиск по файлам

### Где что находится:

| Что изменить | Файл | Строка |
|--------------|------|--------|
| **Телефон** | `src/components/Header.tsx` | ~50 |
| **Email** | `src/components/Footer.tsx` | ~88 |
| **Главный заголовок** | `src/components/Hero.tsx` | ~30 |
| **Цены** | `src/components/PriceList.tsx` | ~9 |
| **Виды бытовок** | `src/components/BytovkiTypes.tsx` | ~8 |
| **Преимущества** | `src/components/Advantages.tsx` | ~20 |
| **FAQ** | `src/components/FAQ.tsx` | ~15 |
| **Этапы работы** | `src/components/HowWeWork.tsx` | ~9 |
| **Меню навигации** | `src/components/Header.tsx` | ~13 |
| **Подвал сайта** | `src/components/Footer.tsx` | ~20 |

---

## ⚠️ Частые ошибки

### 1. Забыли запятую

❌ **Неправильно:**
```typescript
{
  name: 'Бытовка 1',
  price: 'от 100 000'
}
{  // Ошибка - нет запятой после предыдущего блока
  name: 'Бытовка 2',
  price: 'от 200 000'
}
```

✅ **Правильно:**
```typescript
{
  name: 'Бытовка 1',
  price: 'от 100 000'
},  // ← Запятая!
{
  name: 'Бытовка 2',
  price: 'от 200 000'
}
```

### 2. Удалили кавычки

❌ **Неправильно:**
```typescript
title: Мой заголовок  // Нет кавычек
```

✅ **Правильно:**
```typescript
title: 'Мой заголовок'  // В кавычках
```

### 3. Неправильный путь к фото

❌ **Неправильно:**
```typescript
image: 'bytovka.jpg'  // Без слэша
```

✅ **Правильно:**
```typescript
image: '/bytovka.jpg'  // Со слэшем в начале
```

---

## 💡 Советы

1. **Делайте резервные копии** перед большими изменениями
2. **Тестируйте локально** командой `npm run dev` перед публикацией
3. **Делайте понятные коммиты** в Git: "Изменены цены" вместо "правки"
4. **Не удаляйте служебные файлы** (node_modules, .git, .next)
5. **Используйте редактор с подсветкой** синтаксиса (VS Code, Cursor)

---

## 🆘 Если что-то сломалось

### Откатить изменения:

```bash
# Посмотреть последние коммиты
git log --oneline

# Откатиться на предыдущий коммит
git reset --hard HEAD~1
git push -f
```

### Проверить ошибки локально:

```bash
# Запустить сайт локально
npm run dev

# Открыть в браузере
# http://localhost:3000
```

---

## 📞 Полезные команды

```bash
# Запустить сайт локально для тестирования
npm run dev

# Собрать production версию
npm run build

# Проверить код на ошибки
npm run lint

# Посмотреть изменения
git status
git diff

# История изменений
git log --oneline
```

---

## 🎓 Учебные ресурсы

- **React документация:** https://react.dev
- **Next.js документация:** https://nextjs.org/docs
- **Tailwind CSS:** https://tailwindcss.com/docs
- **Git основы:** https://githowto.com/ru

---

**Готово! Теперь вы можете легко редактировать любую часть сайта! 🎉**

Если возникнут вопросы - всегда можно использовать глобальный поиск (`Cmd+Shift+F`) для поиска нужного текста в проекте.
