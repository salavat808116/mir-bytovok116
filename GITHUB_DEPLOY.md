# 🚀 Загрузка сайта на GitHub

## ✅ Что уже сделано:
- [x] Git репозиторий инициализирован
- [x] Все файлы добавлены
- [x] Первый коммит создан (30 файлов, 5805+ строк)

---

## 📋 Пошаговая инструкция

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте браузер и перейдите на [github.com](https://github.com)
2. Войдите в свой аккаунт (или создайте новый, если его нет)
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"**

**Настройки репозитория:**
- **Repository name:** `mir-bytovok` (или любое другое название)
- **Description:** "Корпоративный сайт компании Мир бытовок - производство и аренда бытовок в Казани"
- **Visibility:** 
  - ✅ **Public** (если хотите, чтобы код был виден всем)
  - или **Private** (если код должен быть приватным)
- **НЕ ставьте галочки:** "Add README", "Add .gitignore", "Choose a license" (у нас уже всё есть)
- Нажмите **"Create repository"**

---

### Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Используйте команды ниже:

#### Вариант A: Через HTTPS (проще, но нужен пароль каждый раз)

```bash
cd /Users/user/Desktop/mir_bytovok

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш username)
git remote add origin https://github.com/YOUR_USERNAME/mir-bytovok.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

#### Вариант B: Через SSH (сложнее настроить, но удобнее в долгосрочной перспективе)

```bash
cd /Users/user/Desktop/mir_bytovok

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш username)
git remote add origin git@github.com:YOUR_USERNAME/mir-bytovok.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

**Если используете SSH в первый раз:**
1. Сгенерируйте SSH ключ: `ssh-keygen -t ed25519 -C "your_email@example.com"`
2. Скопируйте ключ: `cat ~/.ssh/id_ed25519.pub`
3. Добавьте в GitHub: Settings → SSH and GPG keys → New SSH key

---

### Шаг 3: Проверьте результат

После успешной загрузки:
1. Обновите страницу вашего репозитория на GitHub
2. Вы должны увидеть все файлы проекта
3. README.md будет отображаться на главной странице

---

## 🔄 Как обновлять код в будущем

После внесения изменений в проект:

```bash
cd /Users/user/Desktop/mir_bytovok

# Посмотреть изменения
git status

# Добавить изменённые файлы
git add .

# Создать коммит
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

---

## 🌐 Автоматический деплой на Vercel (бонус)

После загрузки на GitHub можете развернуть сайт на Vercel **бесплатно**:

### Вариант 1: Через веб-интерфейс

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"** → выберите **"Continue with GitHub"**
3. Нажмите **"Import Project"**
4. Выберите репозиторий `mir-bytovok`
5. Нажмите **"Deploy"**
6. Готово! Сайт будет доступен по адресу `mir-bytovok.vercel.app`

### Вариант 2: Через командную строку

```bash
# Установите Vercel CLI
npm install -g vercel

# Войдите в аккаунт
vercel login

# Задеплойте проект
cd /Users/user/Desktop/mir_bytovok
vercel

# Для production деплоя
vercel --prod
```

**Преимущества Vercel:**
- ✅ Бесплатный хостинг
- ✅ HTTPS автоматически
- ✅ CDN по всему миру
- ✅ Автоматический деплой при каждом push в GitHub
- ✅ Оптимизация для Next.js

---

## 📝 Полезные команды Git

```bash
# Посмотреть статус
git status

# Посмотреть историю коммитов
git log --oneline

# Посмотреть удалённые репозитории
git remote -v

# Откатить последний коммит (осторожно!)
git reset --soft HEAD~1

# Посмотреть изменения
git diff

# Создать новую ветку
git checkout -b feature-name

# Переключиться на другую ветку
git checkout main
```

---

## ⚠️ Важные заметки

### Что НЕ нужно загружать на GitHub:

Эти файлы/папки уже исключены через `.gitignore`:
- ❌ `node_modules/` - зависимости (слишком большие)
- ❌ `.next/` - файлы сборки
- ❌ `.env.local` - секретные данные (пароли, API ключи)
- ❌ `package-lock.json` - можно не загружать, но мы загрузили для точности версий

### Безопасность:

⚠️ **Никогда не загружайте на GitHub:**
- Пароли
- API ключи
- Токены доступа
- Данные кредитных карт
- Личную информацию клиентов

Если случайно загрузили секреты:
1. Удалите из кода
2. Смените пароли/ключи
3. Очистите историю Git или сделайте репозиторий приватным

---

## 🎯 Что дальше?

После загрузки на GitHub:

1. ✅ **Настройте GitHub Pages** (если нужен статический хостинг)
2. ✅ **Подключите Vercel** для автоматического деплоя
3. ✅ **Добавьте бейдж билда** в README
4. ✅ **Настройте GitHub Actions** для автоматического тестирования
5. ✅ **Пригласите других разработчиков** (Settings → Collaborators)

---

## 💡 Полезные ссылки

- [GitHub Documentation](https://docs.github.com)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)
- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)

---

**Готово! Теперь ваш проект на GitHub! 🎉**
