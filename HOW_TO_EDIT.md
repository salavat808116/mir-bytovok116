# 📝 Как редактировать сайт

## 🎯 Процесс редактирования

Теперь когда сайт на домене **mir-bytovok116.ru**, вот как его обновлять:

### Шаг 1: Редактируйте файлы на компьютере

1. Откройте папку проекта: `/Users/user/Desktop/mir_bytovok116`
2. Редактируйте нужные файлы (см. структуру ниже)
3. Сохраните изменения

### Шаг 2: Сохраните изменения в Git

```bash
cd /Users/user/Desktop/mir_bytovok116

# Посмотрите что изменилось
git status

# Добавьте изменения
git add .

# Создайте коммит с описанием
git commit -m "Описание изменений"

# Загрузите на GitHub
git push
```

### Шаг 3: Vercel автоматически обновит сайт

- Vercel следит за вашим GitHub репозиторием
- При каждом `git push` автоматически запускается деплой
- Через 1-2 минуты изменения появятся на сайте
- Вы получите уведомление на email о статусе деплоя

---

## 📁 Структура проекта - Что где редактировать

### 🏠 Главная страница
**Файл:** `src/app/page.tsx`
- Содержит всю главную страницу
- Импортирует компоненты: Hero, Advantages, Services, BytovkiTypes

### 📄 Остальные страницы

| Страница | Файл | URL |
|----------|------|-----|
| О компании | `src/app/about/page.tsx` | /about |
| Продажа | `src/app/sale/page.tsx` | /sale |
| Аренда | `src/app/rent/page.tsx` | /rent |
| Производство | `src/app/production/page.tsx` | /production |
| Манипулятор | `src/app/manipulator/page.tsx` | /manipulator |
| Заявки | `src/app/applications/page.tsx` | /applications |
| Контакты | `src/app/contacts/page.tsx` | /contacts |
| Политика конфиденциальности | `src/app/privacy/page.tsx` | /privacy |

### 🧩 Компоненты (переиспользуемые блоки)

Находятся в `src/components/`:

| Компонент | Файл | Описание |
|-----------|------|----------|
| Header | `Header.tsx` | Шапка сайта с навигацией |
| Footer | `Footer.tsx` | Подвал сайта |
| Hero | `Hero.tsx` | Главный баннер с фото |
| Advantages | `Advantages.tsx` | Блок "Наши преимущества" |
| Services | `Services.tsx` | Блок "Наши услуги" |
| BytovkiTypes | `BytovkiTypes.tsx` | Блок "Виды бытовок" |
| ContactForm | `ContactForm.tsx` | Форма обратной связи |

### 🎨 Стили
**Файл:** `src/app/globals.css`
- Глобальные стили
- Tailwind CSS настройки
- Кастомные цвета и шрифты

### ⚙️ Конфигурация

| Файл | Описание |
|------|----------|
| `src/app/layout.tsx` | Общий макет (layout) для всех страниц |
| `next.config.js` | Настройки Next.js |
| `tailwind.config.ts` | Настройки Tailwind CSS |
| `package.json` | Зависимости проекта |

---

## 🔧 Типичные задачи редактирования

### Изменить текст на главной странице

1. Откройте `src/app/page.tsx`
2. Найдите нужный текст
3. Измените его
4. Сохраните файл
5. Выполните команды:
```bash
git add .
git commit -m "Обновлен текст на главной"
git push
```

### Изменить номер телефона или адрес

**Телефон и адрес в шапке сайта:**
- Откройте `src/components/Header.tsx`
- Найдите строки с телефоном: `+7 (999) 999-99-99`
- Замените на свой номер

**Телефон на странице контактов:**
- Откройте `src/app/contacts/page.tsx`
- Измените контактные данные

**Совет:** Используйте поиск (Cmd+F) чтобы найти все места с телефоном

### Изменить фото

1. Добавьте новое фото в папку `public/`
2. В нужном файле измените путь к фото:
```typescript
<img src="/ваше-фото.jpg" alt="Описание" />
```

### Добавить новую страницу

1. Создайте папку в `src/app/`, например `src/app/novaya-stranica/`
2. Создайте файл `page.tsx` внутри этой папки
3. Скопируйте структуру из любой существующей страницы
4. Отредактируйте контент
5. Страница автоматически станет доступна по адресу `/novaya-stranica`

### Изменить цвета сайта

Откройте `tailwind.config.ts` и измените цвета:

```typescript
colors: {
  primary: '#ваш-цвет',
  secondary: '#ваш-цвет',
}
```

---

## 🚀 Быстрые команды

### Посмотреть изменения
```bash
cd /Users/user/Desktop/mir_bytovok116
git status
git diff
```

### Запустить сайт локально (для тестирования)
```bash
cd /Users/user/Desktop/mir_bytovok116
npm run dev
```
Откройте в браузере: `http://localhost:3000`

### Отменить последний коммит (если ошиблись)
```bash
git reset --soft HEAD~1
```

### Посмотреть историю изменений
```bash
git log --oneline
```

---

## 📊 Отслеживание деплоев в Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Откройте ваш проект **mir-bytovok116**
3. На вкладке **"Deployments"** видны все деплои
4. Можете посмотреть логи сборки
5. Откатиться на предыдущую версию если нужно

---

## ⚠️ Важные замечания

### ❌ НЕ редактируйте файлы напрямую на GitHub
- Всегда редактируйте локально на компьютере
- Используйте Git для сохранения изменений
- Это обеспечит правильную историю и возможность отката

### ❌ НЕ удаляйте служебные файлы
- `node_modules/` - не трогайте (генерируется автоматически)
- `.next/` - не трогайте (генерируется при сборке)
- `.git/` - не трогайте (история Git)
- `package.json`, `next.config.js` - редактируйте аккуратно

### ✅ Тестируйте локально перед деплоем
```bash
npm run dev
```
Проверьте изменения на `localhost:3000` перед `git push`

### ✅ Делайте понятные коммиты
```bash
# Плохо
git commit -m "изменения"

# Хорошо
git commit -m "Обновлен номер телефона в шапке"
git commit -m "Добавлены новые фото бытовок"
git commit -m "Исправлена форма обратной связи"
```

---

## 🆘 Если что-то сломалось

### Сайт не открывается после деплоя

1. Проверьте деплой в Vercel - есть ли ошибки?
2. Посмотрите логи сборки
3. Откатитесь на предыдущую версию:
   - В Vercel → Deployments
   - Найдите последний рабочий деплой
   - Нажмите "..." → "Promote to Production"

### Ошибки в коде

1. Откройте терминал в папке проекта
2. Запустите локально: `npm run dev`
3. Посмотрите ошибки в терминале
4. Исправьте ошибки
5. Проверьте снова локально
6. Сделайте `git push`

### Нужна помощь

- Документация Next.js: https://nextjs.org/docs
- Документация Vercel: https://vercel.com/docs
- Документация Tailwind CSS: https://tailwindcss.com/docs
- GitHub репозиторий: https://github.com/salavat808116/mir-bytovok116

---

## 📚 Полезные ссылки

- **Ваш сайт:** https://mir-bytovok116.ru
- **Vercel Dashboard:** https://vercel.com/dashboard
- **GitHub репозиторий:** https://github.com/salavat808116/mir-bytovok116
- **Vercel деплои:** https://vercel.com/salavat808116/mir-bytovok116

---

## 🎓 Пример: Изменение текста на главной

### Было:
```typescript
<h1>Мир бытовок</h1>
<p>Производство и аренда бытовок в Казани</p>
```

### Стало:
```typescript
<h1>Мир бытовок 116</h1>
<p>Качественные бытовки от производителя</p>
```

### Как применить:
```bash
cd /Users/user/Desktop/mir_bytovok116
# Отредактируйте файл в любом редакторе
git add src/app/page.tsx
git commit -m "Обновлен заголовок на главной странице"
git push
```

Через 2 минуты изменения появятся на https://mir-bytovok116.ru 🎉

---

**Готово! Теперь вы можете редактировать сайт! 🚀**
